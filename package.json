{
  "name": "youtube-tv-xinput",
  "version": "2.0.0",
  "private": true,
  "description": "YouTube TV (leanback) client built with Pake, with Xbox/XInput controller support and a native ad blocker",
  "scripts": {
    "prepare:runtime": "node scripts/prepare.cjs",
    "build": "node scripts/build.cjs",
    "test": "cargo test --locked --manifest-path native/core/Cargo.toml"
  },
  "devDependencies": {
    "pake-cli": "3.15.7"
  }
}
